local Menu = require("nui.menu")
local menuName = "Dll Menu"

local function createDllMenu()
	local menu = Menu({
		position = "50%",
		size = {
			width = 30,
			height = 15,
		},
		border = {},
	})
end
